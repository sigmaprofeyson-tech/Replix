services:
  - type: web
    name: replix
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: ADMIN_TOKEN
        value: replix-admin-123
      - key: AUTO_APPROVE
        value: "1"
